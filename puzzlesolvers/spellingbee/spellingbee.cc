#include "puzzlesolvers/spellingbee/spellingbee.h"

namespace puzzlesolvers {
  
std::vector<std::string> SpellingBee::FindWords(std::set<char> characters) {
  // First, find the intersection of all words that 
}

}  // namespace puzzlesolvers